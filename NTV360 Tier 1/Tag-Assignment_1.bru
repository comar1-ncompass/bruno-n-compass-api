meta {
  name: Tag-Assignment_1
  type: http
  seq: 109
}

post {
  url: {{API_URL}}/tag-assignments
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
      "tagId": "bfa5756b-387b-4560-8787-71a07b8989e0",
      "entitySlug": "territories",
      "recordId": "d290f1ee-6c54-4b01-90e6-d701748f085a",
      "createdBy": "f1e4eacb-401c-4af0-b530-a7e58431f3c9"
  }
}

script:pre-request {
  // Configure range
  let start = 1;
  let end = 50;
  
  let current = bru.getEnvVar("userIndex");
  
  if (!current) {
      current = start;
  } else {
      current = parseInt(current) + 1;
  }
  
  // Stop runner if we've reached the end
  if (current > end) {
      bru.setNextRequest(null); // halts the runner
  } else {
      bru.setEnvVar("userIndex", current);
  
      const username = `qwerty${current}`;
      const email = `${username}@example.com`;
  
      bru.setEnvVar("username", username);
      bru.setEnvVar("email", email);
  }
  
}

settings {
  encodeUrl: true
}
