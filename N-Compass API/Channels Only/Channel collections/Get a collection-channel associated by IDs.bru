meta {
  name: Get a collection-channel associated by IDs
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/channel-collections/{{standalone_collectionId}}/{{standalone_channelId_A}}
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{bearer_token}}
  Content-Type: text/plain
}

script:post-response {
  // 1. Check if the status code is 200
  test("Status code is 200", function () {
      expect(res.getStatus()).to.equal(200);
  });
  
  // 2. Check if the response time is acceptable (under 5 seconds)
  test("Response time is acceptable", function () {
      expect(res.getResponseTime()).to.be.below(5000);
  });
  
  // 3. Check if the response is a valid JSON object
  test("Response is valid JSON array", function () {
      expect(res.getBody()).to.exist;
      const responseJson = res.getBody();
      expect(responseJson).to.be.an('object');
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
